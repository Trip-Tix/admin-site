const login = '/login';
const logout = '/logout';
const home = '/dashboard';
const list = '/list';
const list_bus = '/list/bus';
const list_train = '/list/train';
const list_flight = '/list/flight';
const add = '/add';
const add_bus = '/add/bus';
const add_train = '/add/train';
const add_flight = '/add/flight';
const schedule = '/schedule';
const schedule_bus = '/schedule/bus';
const schedule_train = '/schedule/train';
const schedule_flight = '/schedule/flight';


export {
    login as login_url,
    logout as logout_url,
    home as home_url,
    list as list_url,
    list_bus as list_bus_url,
    list_train as list_train_url,
    list_flight as list_flight_url,
    add as add_url,
    add_bus as add_bus_url,
    add_train as add_train_url,
    add_flight as add_flight_url,
    schedule as schedule_url,
    schedule_bus as schedule_bus_url,
    schedule_train as schedule_train_url,
    schedule_flight as schedule_flight_url
};